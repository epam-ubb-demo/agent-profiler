---
title: Writing Documentation
description: How to add and update pages on the Agent Profiler documentation site — frontmatter format, file locations, and local build instructions.
---

The Agent Profiler documentation site is built with [Astro](https://astro.build/) and [Starlight](https://starlight.astro.build/). All documentation lives in `docs/src/content/docs/` within the monorepo.

## File Format

All documentation pages use the `.mdx` extension (MDX = Markdown + JSX). Every page must begin with a frontmatter block:

```mdx
---
title: My Page Title
description: A concise one-sentence description for search engines and SEO.
---

Page content begins here.
```

Both `title` and `description` are **required**.

## Directory Structure

Pages are organised by section under `docs/src/content/docs/`:

```
docs/src/content/docs/
├── index.mdx              # Site home page
├── guides/                # Getting started and tutorials
├── otel-gateway/          # OTel Gateway documentation
├── desktop/               # Desktop app reference
├── adapters/              # Adapter documentation
├── decisions/             # Architecture Decision Records
├── operations/            # Release runbooks and ops guides
└── contributing/          # Contributor guides (you are here)
```

Each directory should have an `index.mdx` that introduces the section and links to its pages.

## Internal Links

Internal links must include the full base path prefix `/agent-profiler/`:

```mdx
<!-- Correct -->
See [OTel Gateway](/agent-profiler/otel-gateway/) for setup details.

<!-- Incorrect — missing base path -->
See [OTel Gateway](/otel-gateway/) for setup details.
```

Starlight's links validator plugin will report broken links during the build.

## Adding a New Page

1. Create a `.mdx` file in the appropriate section directory.
2. Add the required frontmatter (`title` and `description`).
3. Update the parent section's `index.mdx` to link to the new page.
4. Run the build locally to verify there are no broken links.

## Adding a New ADR

ADRs live in `docs/src/content/docs/decisions/`. Use the naming convention:

```
adr-<NNNN>-<short-slug>.mdx
```

After creating the ADR file, add a row to the table in [`decisions/index.mdx`](/agent-profiler/decisions/).

## Building Locally

```bash
cd docs
npm run dev     # Start the dev server with hot reload
npm run build   # Production build (runs the links validator)
```

The dev server starts at `http://localhost:4321/agent-profiler/`.

## Style Guide

- Write in **British English** (colour, organisation, behaviour, summarise, artefact).
- Use sentence case for headings.
- Prefer tables for structured reference data.
- Use [Starlight admonitions](https://starlight.astro.build/guides/authoring-content/#asides) (`:::tip`, `:::caution`, `:::note`) for callouts.
- Code blocks should specify the language for syntax highlighting.
- Use `:::note[Coming Soon]` for stub pages that are not yet fully written.

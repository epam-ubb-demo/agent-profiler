name: Release Electron App

# Production trigger: a tag matching `@agent-profiler/desktop@X.Y.Z` pushed to
# main. Delegates to the reusable workflow so the dry-run pipeline shares the
# exact same job graph.

on:
  push:
    tags:
      - '@agent-profiler/desktop@*'

permissions:
  contents: read

concurrency:
  group: release-electron-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    uses: ./.github/workflows/release-electron.reusable.yml
    permissions:
      contents: write
    with:
      tag-name: ${{ github.ref_name }}
      dry-run: false
      platforms: macos,windows,linux
    secrets: inherit

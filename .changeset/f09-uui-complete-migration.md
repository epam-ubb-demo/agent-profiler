---
"@agent-profiler/ui": minor
---

Complete EPAM UUI migration for all remaining components (panels, fanout, comparative). Zero Tailwind utility classes remain — all styling now uses UUI components, FlexRow/Panel/Text/Badge/Button primitives, and `--uui-*` CSS variables for full theme compliance (light + dark).
